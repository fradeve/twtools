set title "$activity | Minutes spent every $period"
set auto x
set yrange [0:$max]
set style data histogram
set style histogram clustered gap 1.5
set style fill solid border -1
set boxwidth 1.2
set offset 5,5,0,0
#plot '/tmp/twtools_timing' using 2:xticlabels(1) with histogram
plot '/tmp/twtools_timing' using 0:2:xtic(1) with boxes, '' using 0:2:2 with labels offset 0, char 1